import "./index.css"
import React from 'react'

const Title = props => (
    <div className="row">
        <h1 className = "col-md-12">Clicky Game</h1>
        <h4 className = "col-md-12">Click each image, but only once!.</h4>
    </div>
)
  
  export default Title